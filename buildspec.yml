version: 0.1

phases:
  build:
    commands:
      - echo Nothing to do yet
artifacts:
  files:
    - index.html
    - favicon.ico
    - Moath-Malkawi-CV-new.pdf
    - styles/*
    - images/*
    - buildspec.yml
    - upload-portfoli-lambda.py
    - babel.html
